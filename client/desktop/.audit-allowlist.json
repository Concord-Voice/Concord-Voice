{
  "//": "Tracked HIGH/CRITICAL npm-audit advisory exceptions for client/desktop. Consumed by scripts/security/npm-audit-gate.{sh,mjs} via the build.yml 'Dependency audit' step. PREFER bumping the dependency (an `overrides` entry in package.json) over accepting. Only add an entry here when no fixed version exists yet. Each entry: { \"id\": \"GHSA-xxxx-xxxx-xxxx\", \"reason\": \"why accepted + tracking link\", \"review_by\": \"YYYY-MM-DD\" }. An expired review_by emits a CI warning (never a block). See [internal]quality-gate-definition.md 'npm-audit gate'.",
  "accepted": [
    {
      "id": "GHSA-MH99-V99M-4GVG",
      "reason": "brace-expansion DoS (unbounded expansion -> OOM) — development-time only: reached solely via the eslint@10.7.0 -> minimatch@9.0.9 -> brace-expansion@2.1.2 devDependency chain, never bundled into the renderer or main process, and exploiting it requires local control of a glob fed to eslint. No compatible fix exists: 2.1.2 is the newest 2.x (already pinned in overrides) and first-patched is 5.0.8, but overriding to ^5.0.8 breaks eslint ('brace_expansion_1.default is not a function' — 5.x dropped the default export minimatch@9 calls), and bumping eslint to 10.8.0 while relaxing the minimatch/brace-expansion overrides fragments the tree (minimatch 3.1.5/5.1.9/9.0.9 reappear) and still fails the gate. Both approaches were tested and reverted. Tracked: https://github.com/Concord-Voice/Concord-Voice-Alpha/issues/2441",
      "review_by": "2026-10-22"
    }
  ]
}
